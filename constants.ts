import { Dimensions, ScaledSize } from "react-native";
const width: number = Dimensions.get("window").width;

export const CONSTANTS = {
  RATE_COLOR: "rgb(245, 197, 24)",
  CARD_SIZE: width * 0.75,
};
